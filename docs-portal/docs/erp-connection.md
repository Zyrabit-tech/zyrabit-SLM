# Connect Your ERP

Use Zyrabit as a local bridge:

- Push ERP exports into ingestion jobs.
- Query via secure `/v1/chat`.
- Expose curated tools/resources through `/mcp`.

Keep sensitive datasets under MCP allowed roots and sanitize outputs by default.
